lire BUFFER_SIZE characteres
si il y a un \n je m'arrete parce que j'ai une ligne
si il y'a pas de \n, je recommence a partir de la 1
retourner 1 ligne (de debut a \n)
stack = reste

while (ft_is_new_line(str) == 0)
{
    i = read(fd, buff, BUFFER_SIZE);
    buff[i] = '\0';
    strjoin(str, buff);
    if (!strjoin)
    {
        free();
        return (NULL);
    }
}
first_line(str);
variable_static = ft_reste_de_str(str); //contient un free de tout ce qui est dans first_line
return (first_line);




bonjour commennt ca va ?\n j'\0
first_line = bonjour comment ca va ?\n\0
reste =  j'\0